{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "micern",
  "compatibility_date": "2026-05-23",
  "compatibility_flags": ["nodejs_compat"],
  "main": "worker/index.ts",
  // Build the SPA into dist/ before deploying. This makes `wrangler deploy`
  // self-contained, so the deploy works without a separate dashboard build
  // command (wrangler still bundles worker/index.ts itself afterwards).
  "build": {
    "command": "npm run build"
  },
  "assets": {
    "directory": "dist",
    "binding": "ASSETS",
    // Serve index.html for unknown client-side routes (/about, /services, …)
    // so hard refreshes and deep links work. Replaces the _redirects SPA rule,
    // which the Workers asset deployer rejects as an infinite loop.
    "not_found_handling": "single-page-application"
  },
  "observability": {
    "enabled": true
  }
}
