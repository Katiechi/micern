# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment / secrets
.env
.env.*

# Lockfile is intentionally untracked: it is regenerated per-platform on the
# build server, which avoids the npm optional-native-dependency bug
# (rollup / lightningcss / tailwind oxide) when building on Linux.
package-lock.json

# Original source photos (large, includes videos) — keep as a local backup.
# The optimized images the site actually uses live in public/images/.
M-CERN photos/

# Bun lockfile removed — project uses npm; build server installs fresh.
bun.lock
bun.lockb
